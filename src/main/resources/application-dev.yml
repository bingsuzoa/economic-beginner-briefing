briefing:
  dry-run: true
  scheduler:
    enabled: false

spring:
  jpa:
    show-sql: true

auth:
  require-https: false

server:
  servlet:
    session:
      cookie:
        secure: false

logging:
  level:
    com.economicbriefing: DEBUG
